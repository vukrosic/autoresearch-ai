name: lr-grid
base_command: "printf 'val_loss={lr} batch_size={batch_size}\n'"
strategy: grid
budget: 4
metric: val_loss
direction: lower
params:
  lr: {type: choice, values: [0.0001, 0.001]}
  batch_size: {type: choice, values: [32, 64]}
